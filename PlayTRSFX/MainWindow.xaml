<Window x:Class="PlayTRSFX.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlayTRSFX"
        mc:Ignorable="d"
        Title="Tomb Raider SFX Player" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="0,0,0,10">
            <Label x:Name="TitleLabel" Content="Tomb Raider SFX Player" FontWeight="Bold" VerticalAlignment="Center" FontSize="24" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10" HorizontalAlignment="Left">
            <Button Name="OpenButton" Click="OpenButton_Click" Width="120" Height="28" Margin="0,0,10,0">Open SFX File...</Button>
            <Button Name="PlayButton" Click="PlayButton_Click" Width="80" Height="28" Margin="0,0,10,0">Play</Button>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListBox Name="SoundsList" Grid.Column="0" Margin="0,0,10,0" MouseDoubleClick="SoundsList_MouseDoubleClick" />
            <TextBox Name="DetailsBox" Grid.Column="1" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
